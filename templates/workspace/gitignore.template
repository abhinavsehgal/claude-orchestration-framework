# Child repo clones — disposable; workspace.json is the source of truth for which repos belong.
# One line per repo path from workspace.json:
/<REPO_1_PATH>/
/<REPO_2_PATH>/
# Delegation artefacts
/.claude/handoffs/
/.claude/returns/
.claude-pre-bootstrap-backup/
